class Counter {
  var count = 0.;
  method inc() {
    count = count + 1.;
    call print(count);
    send self.dec(3.);
  }
  method dec(x) {
    count = count - x;
    call print(count);
  }
}

var c1 = new Counter();
var c2 = new Counter();
send c1.inc();
send c2.inc();
