// 期待（警告）: 期限の無い select は、来なければ永久に待つ。
// now / await と同じ扱い ---- 既定は警告、AIOS_STRICT_DEADLINE=1 でエラー。
class W1 {
  method j(k) : string { reply(k); }
  method serve() : unit !{log} { select { case j(k) -> { reply(k); } } }
}
var w = new W1();
send w.serve();
send w.j("x");
